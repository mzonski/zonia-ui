import { ThemePrimaryColor, ThemeShapeType } from '@zonia-ui/theme';

const toggleSize = [''];

export type StyledToggle = {
  color?: ThemePrimaryColor;
  outlineColor?: ThemePrimaryColor;
  borderColor?: ThemePrimaryColor;
  shape?: ThemeShapeType;
};
